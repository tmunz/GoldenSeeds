.extended-range-input {
  position: relative;

  .range-input {
    position: absolute;
    bottom: -4px;
    padding: 0;
    margin: 0;

    &>input {
      height: 4px;
      padding: 0;
      margin: 0;
    }
  }
}