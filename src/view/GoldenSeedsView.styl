.golden-seeds-view {
  width: 100%;
  height: 100%;
  .canvas {
    position: fixed;
    top: 50%;
    left: 50%;
    transition: left 1.2s ease;
    transform: translate(-50%, -50%);
  }
  .overlay {
    position: absolute;
    padding: 20px;
    .colapsable {
      overflow: hidden;
      transition: height 0.6s;
    }
    &.editor {
      top: 0;
      left: 0;
    }
    &.toolbar {
      top: 0;
      right: 0;
    }
    &.themer {
      bottom: 0;
      right: 0;
      h1 {
        text-align: right;
      }
    }
  }
}